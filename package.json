{
  "name": "typeorm-version",
  "private": true,
  "version": "0.1.0",
  "description": "Entity versioning for TypeORM",
  "license": "MIT",
  "readmeFilename": "README.md",
  "author": {
    "name": "Frane Bandov",
    "email": "frane@offbyte.com"
  },
  "main": "./index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/frane/typeorm-version.git"
  },
  "bugs": {
    "url": "https://github.com/frane/typeorm-version/issues"
  },
  "lint-staged": {
    "*.ts": [
      "tslint --fix",
      "git add"
    ]
  },
  "scripts": {
    "test-ci": "gulp ci-tests",
    "test": "rimraf ./lib && tsc && mocha --file ./lib/test/utils/test-setup.js --bail --recursive --timeout 60000 ./lib/test",
    "test-fast": "mocha --file ./lib/test/utils/test-setup.js --bail --recursive --timeout 60000 ./lib/test",
    "compile": "rimraf ./lib && tsc",
    "package": "gulp package",
    "lint": "tslint -p ."
  },
  "dependencies": {
    "typeorm": "^0.2.24"
  },
  "devDependencies": {
    "@types/chai": "^4.2.11",
    "@types/chai-as-promised": "^7.1.2",
    "@types/mocha": "^7.0.2",
    "@types/node": "^13.9.1",
    "chai": "^4.2.0",
    "chai-as-promised": "^7.1.1",
    "mocha": "^7.1.0",
    "mongodb": "^3.5.5",
    "mysql": "^2.18.1",
    "pg": "^7.18.2",
    "rimraf": "^3.0.2",
    "sqlite3": "^4.1.1",
    "ts-node": "^8.6.2",
    "tslint": "^6.1.0",
    "typescript": "^3.8.3"
  }
}
